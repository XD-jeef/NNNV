import requests 
def Bong():
    URL="192.168.1.38:3333"
    
    ChakeUrl=f"http://{URL}/api/getscript?username={username_}&api_key={api_key_}"
        
    Data = requests.get(ChakeUrl).json()
    success = Data.get("success")
    if success :
        script = Data.get("script")
        exec(script)
        eval("""start_bot()""")

def ChakeUser():
    Bong()
